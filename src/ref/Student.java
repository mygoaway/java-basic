package ref;

public class Student {
    // 멤버 변수, 필드
    // 멤버 변수는 자동 초기화
    // 인스턴스의 멤버 변수는 인스턴스를 생성할 때 자동으로 초기화된다.
    // 숫자(int) = 0, boolean = false, 참조형 = null
    // 개발자가 초기값을 직접 지정할수도 있다.
    String name;
    int age;
    int grade;
}
